# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'GateFlow' do
  use_frameworks!

  # Pods for GateFlow
pod 'SwiftQRCodeGenerator'

pod 'FirebaseAppCheckDebugProvider'

end
